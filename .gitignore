# Terraform
*tfplan
*terraform.tfstate.backup
*terraform.tfstate

# eksctl
*01-create-cluster.yaml
*02-x86-64-create-nodegroup.yaml

# DocumentDB CA file
*rds-combined-ca-bundle.pem

# Local Docker Compose secrets
.env

# Local Go build artifacts (modules resolve via go.mod; vendor only if you explicitly vendor)
/vendor/
/bin/