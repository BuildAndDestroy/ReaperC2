apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: reaperc2-ns

# Same DocumentDB + app stack as EKS; no ECR imagePullSecret (use a public image,
# in-cluster registry, or add your own secret + patch).
resources:
  - ../../base
