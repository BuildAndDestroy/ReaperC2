# EKS / any cluster pulling from ECR with docker-registry secret reaperc2-myregistrykey.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: reaperc2-deployment
spec:
  template:
    spec:
      imagePullSecrets:
        - name: reaperc2-myregistrykey
