# Template only — copy to documentdb-secret.local.yaml, fill in values, apply the .local file.
# Do not commit real hostnames or passwords to git.
# Deploy order: ../README.md#run-from-scratch-checklist
apiVersion: v1
kind: Secret
metadata:
  name: reaperc2-documentdb-credentials
  namespace: reaperc2-ns
type: Opaque
stringData:
  host: "YOUR_CLUSTER_ID.cluster-XXXXXXXX.us-east-1.docdb.amazonaws.com"
  port: "27017"
  username: "api_user"
  password: "CHANGE_ME"
  database: "api_db"
  auth_source: "api_db"
