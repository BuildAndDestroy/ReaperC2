# Template only — copy to documentdb-admin-secret.local.yaml, fill in values, apply the .local file.
# Master/admin user for docdb-init-user-job.yaml only. Do not use in the ReaperC2 deployment.
apiVersion: v1
kind: Secret
metadata:
  name: reaperc2-documentdb-admin
  namespace: reaperc2-ns
type: Opaque
stringData:
  username: "docdbadmin"
  password: "CHANGE_ME"
