# Legacy path: forwards to the shared reaperc2 overlay (ECR pull secret patch).
# Prefer: kubectl apply -k ../reaperc2/overlays/aws-ecr
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../reaperc2/overlays/aws-ecr
